# 기능 현황

앱과 서버 전반의 기능 구현 상태를 추적하는 문서이다.

## 상태 표기

| 상태 | 의미 |
| --- | --- |
| `○ 완료` | 사용자가 실제 흐름을 수행할 수 있는 상태 |
| `△ 진행 중` | 일부 구현됐지만 API, 화면, 예외 처리, 검증 중 하나 이상이 남은 상태 |
| `X 미구현` | 아직 사용자 기능으로 동작하지 않는 상태 |

## 사용자 기능

### 1.1 회원 관리

| 기능 | 상태 | 현재 구현/남은 작업 |
| --- | --- | --- |
| 회원가입 | `○ 완료` | 아이디, 비밀번호, 이름, 전화번호, 성별, 생년월일 입력 기반 회원가입 흐름 구현. |
| 로그인 | `○ 완료` | 아이디와 비밀번호 기반 로그인 흐름 구현. |
| 비밀번호 찾기 | `△ 진행 중` | 아이디, 이름, 생년월일, 전화번호 기반 찾기 기능은 상태 확인 및 서버 계약 검증 필요. |
| 정보 수정 | `△ 진행 중` | 이름, 전화번호, 비밀번호 수정 기능은 화면/API 연동 상태 확인 필요. |

### 1.2 지도

| 기능 | 상태 | 현재 구현/남은 작업 |
| --- | --- | --- |
| 안전 인프라 지도 보기 | `○ 완료` | 현재 화면 bounds 기준으로 CCTV와 경찰서 시설물을 조회해 지도 마커로 표시하는 지도 기본 기능 완료. 가로등, 보안등, 비상벨 등 추가 시설 유형은 확장 작업으로 관리. |
| 도로 안전 등급 확인 | `△ 진행 중` | `/roads/sample` 응답을 도로 라인으로 표시하고 `safety_score`에 따라 색상을 적용. 최종 `/roads` API, `final_score`, 5단계 등급 체계 연동 필요. |

### 1.3 내비게이션

| 기능 | 상태 | 현재 구현/남은 작업 |
| --- | --- | --- |
| 안심 경로 탐색 | `X 미구현` | 출발지와 목적지를 입력해 위험 요소를 최소화한 안심 경로를 안내하는 기능은 아직 미구현. |
| 경로 옵션 설정 | `X 미구현` | 추천 안심 경로와 대로변 우선 경로 선택 기능은 아직 미구현. |

### 1.4 안심 케어

| 기능 | 상태 | 현재 구현/남은 작업 |
| --- | --- | --- |
| 긴급 SOS 요청 | `X 미구현` | 위급 상황 시 사전 등록 연락처로 현재 위치와 도로 정보를 전송하는 기능은 아직 미구현. |

### 1.5 신고

| 기능 | 상태 | 현재 구현/남은 작업 |
| --- | --- | --- |
| 안전 시설물 신고 | `X 미구현` | 가로등 고장, 도로 파손 등 위험 요소를 사진과 함께 신고하는 기능은 아직 미구현. |
| 신고 내역 조회 | `X 미구현` | 사용자가 제보한 신고 내역과 처리 상태 조회 기능은 아직 미구현. |

### 1.6 알림

| 기능 | 상태 | 현재 구현/남은 작업 |
| --- | --- | --- |
| 위험 진입 알림 | `X 미구현` | 위험 등급 도로 진입 시 진동 또는 푸시 알림을 제공하는 기능은 아직 미구현. |
| 알림 내역 조회 | `X 미구현` | 위험 진입 알림, SOS 관련 알림 등의 내역 조회 기능은 아직 미구현. |
| 알림 설정 | `X 미구현` | 위험 진입 알림, SOS 관련 알림 등의 수신 여부 설정 기능은 아직 미구현. |

### 1.7 비상 연락처

| 기능 | 상태 | 현재 구현/남은 작업 |
| --- | --- | --- |
| 비상 연락처 등록 | `X 미구현` | 긴급 SOS 호출 시 문자를 받을 보호자 및 지인 연락처 등록 기능은 아직 미구현. |
| 비상 연락처 수정 | `X 미구현` | 등록된 보호자 이름과 전화번호 수정 기능은 아직 미구현. |
| 비상 연락처 삭제 | `X 미구현` | 더 이상 사용하지 않는 비상 연락처 삭제 기능은 아직 미구현. |

### 1.8 즐겨찾기

| 기능 | 상태 | 현재 구현/남은 작업 |
| --- | --- | --- |
| 즐겨찾기 추가 | `X 미구현` | 자주 방문하는 장소를 등록해 빠르게 목적지를 설정하는 기능은 아직 미구현. |
| 즐겨찾기 삭제 | `X 미구현` | 목록에서 불필요한 즐겨찾기 장소를 삭제하는 기능은 아직 미구현. |

## 구현 상태 요약

| 구분 | 기능 |
| --- | --- |
| 완료 | 회원가입, 로그인, 하단 내비게이션 기본 구조, 지도 중심 메인 화면, 현재 화면 bounds 기반 시설물 마커 로딩, CCTV/경찰서 마커 구분 표시 |
| 진행 중 | 인증 부가 기능, 샘플 도로 안전도 라인 표시, 경로탐색 기능 설계 및 구현 |
| 보류/미구현 | 안심 경로 탐색, 경로 옵션, SOS, 신고, 알림, 비상 연락처, 즐겨찾기 관리 |

## 다음 세미나까지 할 일

- PPT에 점수 산정 알고리즘 설명을 추가한다. 특히 안전 점수를 왜 해당 방식으로 계산했는지 근거와 계산 흐름을 정리한다.
- 지도 기능은 완료된 범위로 정리하고, 이후 작업의 중심을 경로탐색 기능 구현으로 이동한다.
- 출발지와 목적지를 기반으로 안심 경로를 탐색하는 기능의 화면 흐름, 데이터/API 연동 방식, 안전 점수 반영 방식을 구체화한다.

## 현재 중점 작업

- 지도 기능은 현재 구현 범위를 완료 상태로 정리하고, 추가 시설 유형은 후속 확장 항목으로 관리한다.
- 경로탐색 기능을 다음 주요 개발 범위로 두고 출발지/목적지 입력, 경로 후보 조회, 안전 점수 기반 추천 흐름을 설계한다.
- `/roads/sample` 샘플 도로 오버레이를 최종 `/roads` API와 `final_score` 기반 색상 표시로 연결한다.
- `crime_wms.png` 기반 범죄주의구간 감점 계산은 CSV 캐시 방식부터 검증한다.

## 작업 로그

### 2026-05-21

- 완료: 최근 커밋 기준으로 CCTV 마커를 지도 최초 진입 및 카메라 이동 종료 시 현재 화면 bounds로 재조회하는 흐름을 확인했다.
- 완료: `AuthService`가 `features/auth/service` 아래로 이동했고, 하단 내비게이션 터치 영역 확장 변경이 반영되어 있음을 확인했다.
- 진행 중: `lib/features/map/map_screen.dart`에서 시설물 로딩이 CCTV 전용에서 시설물 공통 로딩으로 확장되고, `police_station` 유형은 별도 경찰서 아이콘으로 표시되도록 변경되어 있다.
- 진행 중: `lib/features/map/service/road_overlay_service.dart`가 추가되어 `/roads/sample` 응답을 `NPolylineOverlay`로 표시하고 `safety_score` 값에 따라 빨강/주황/노랑/초록 색상을 적용한다.
- 진행 중: `docs/ROAD_SAFETY_WMS_SCORING_PLAN.md`가 추가되어 범죄주의구간 WMS 이미지 기반 도로 위험 감점 계산 방향을 정리하고 있으나, 파일 내용은 인코딩이 깨져 보여 별도 복구가 필요하다.
- 보류: `docs/WORK_LOG.md`는 현재 워킹트리에서 삭제 상태로 표시된다. 이번 갱신에서는 사용자가 요청한 기능 현황 문서만 최신화하고 삭제 상태는 변경하지 않았다.

### 2026-05-21 추가 정리

- 완료: 사용자 기능 표를 대분류별 섹션으로 나누고, 넓은 5열 표를 `기능 / 상태 / 현재 구현·남은 작업` 중심의 작은 표로 재구성했다.
- 완료: 상태 표기와 구현 상태 요약을 별도 표로 분리해 문서 첫 화면에서 현재 수준을 빠르게 읽을 수 있도록 정리했다.

### 2026-05-21 세미나 기록

- 완료 정리: 지도 기능은 현재 구현 범위를 완료로 보고, 이후 추가 시설 유형은 확장 작업으로 분리한다.
- 다음 작업: 경로탐색 기능을 만들어야 하며, 출발지/목적지 입력과 안전 점수 기반 경로 추천 흐름을 우선 검토한다.
- 발표 준비: PPT에 점수 산정 알고리즘을 추가하고, 왜 현재 방식으로 계산했는지 설명할 근거를 정리한다.
